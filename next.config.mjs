/** @type {import('next').NextConfig} */
const nextConfig = {
  /* Will be used to export the project to static files */
  // output: 'export',
  // reactStrictMode: true,
  // images: {
  //   unoptimized: true,
  // },
};

export default nextConfig;